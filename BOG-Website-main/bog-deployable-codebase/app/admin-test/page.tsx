export default function AdminTestPage() {
  return (
    <main style={{ padding: '40px', color: 'white', background: 'black', minHeight: '100vh' }}>
      <h1>ADMIN TEST ROUTE WORKS</h1>
      <p>If you can see this, the deployment is live and routing is working.</p>
    </main>
  );
}
